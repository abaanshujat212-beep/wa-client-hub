const test=require('node:test');const assert=require('node:assert/strict');const {parseMessage,FACEBOOK_ORIGINS}=require('../public/meta-signup');test('browser accepts exact Meta origins and Cloud API FINISH assets only',()=>{const d={type:'WA_EMBEDDED_SIGNUP',event:'FINISH',data:{waba_id:'123',phone_number_id:'456'}};assert.deepEqual(parseMessage('https://www.facebook.com',d),{kind:'finish',businessAccountId:'123',phoneNumberId:'456'});assert.equal(parseMessage('https://www.facebook.com.evil.test',d),null);assert.deepEqual(parseMessage('https://web.facebook.com',JSON.stringify({...d,event:'CANCEL'})),{kind:'cancel'});assert.deepEqual(FACEBOOK_ORIGINS,['https://www.facebook.com','https://web.facebook.com']);});test('launcher uses code flow without browser token persistence or logging',()=>{const s=require('node:fs').readFileSync(require('node:path').resolve(__dirname,'../public/meta-signup.js'),'utf8');assert.match(s,/config_id:config\.configId/);assert.match(s,/response_type:'code'/);assert.doesNotMatch(s,/localStorage|sessionStorage|console\./);assert.match(s,/Messaging remains disabled/);});
