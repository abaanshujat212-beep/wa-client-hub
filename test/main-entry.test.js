const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');test('production start composes default-gated Meta signup lifecycle and webhook runtimes',()=>{const pkg=JSON.parse(fs.readFileSync(path.resolve(__dirname,'..','package.json')));assert.equal(pkg.main,'src/main.js');assert.equal(pkg.scripts.start,'node src/main.js');const main=fs.readFileSync(path.resolve(__dirname,'..','src/main.js'),'utf8');assert.match(main,/createMetaSignupRuntime/);assert.match(main,/createMetaWebhookRuntime/);assert.match(main,/\/webhooks\/meta\/whatsapp/);assert.match(main,/metaSignup\.start\(\)/);assert.match(main,/metaWebhook\.start\(\)/);assert.match(main,/metaWebhook\.stop\(\)/);});
