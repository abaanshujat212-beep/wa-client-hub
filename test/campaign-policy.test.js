const test=require("node:test");const assert=require("node:assert/strict");
const {parseCsv,isOptOut,renderTemplate,inQuietHours,normalizePhone}=require("../src/campaigns/policy");
test("campaign CSV requires auditable consent evidence",()=>{assert.throws(()=>parseCsv("phone,name\n+923001112222,Ada"),/CSV requires/);assert.throws(()=>parseCsv("phone,consent_source,policy_version,consent_captured_at\nbad,form,v1,2026-01-01"),/Invalid CSV row/);const rows=parseCsv('phone,name,consent_source,policy_version,consent_captured_at,consent_evidence\n"+92 3001112222","Ada, A",web-form,v2,2026-01-01T00:00:00Z,signup-1');assert.equal(rows[0].phone,"+923001112222");assert.equal(rows[0].name,"Ada, A");assert.equal(rows[0].policyVersion,"v2");});
test("campaign policy detects exact opt-outs and safely personalizes",()=>{assert.equal(isOptOut(" STOP "),true);assert.equal(isOptOut("do not stop by"),false);assert.equal(renderTemplate("Hi {{ name }} at {{phone}}",{name:"Ada",phone:"+1"}),"Hi Ada at +1");assert.equal(normalizePhone("+92 (300) 111-2222"),"+923001112222");});
test("quiet hours support same-day and overnight UTC windows",()=>{const atHour=h=>new Date(`2026-01-01T${String(h).padStart(2,"0")}:00:00Z`);assert.equal(inQuietHours(atHour(23),22,8),true);assert.equal(inQuietHours(atHour(12),22,8),false);assert.equal(inQuietHours(atHour(10),9,17),true);});
