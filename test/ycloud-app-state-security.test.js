const test=require('node:test');const assert=require('node:assert/strict');const{normalizeYCloudEvent}=require('../src/messaging/ycloudWebhookNormalizer');const{YCloudWebhookRepository}=require('../src/messaging/ycloudWebhookRepository');test('YCloud app-state is recognized but cannot bypass exact signed asset resolution',async()=>{const event=normalizeYCloudEvent({id:'state-1',type:'whatsapp.smb.app.state.sync',whatsappSmbAppState:{}});assert.equal(event.kind,'app_state');assert.equal(event.businessPhone,'__unroutable__');const repository=new YCloudWebhookRepository({async query(){return{rowCount:0,rows:[]};}},{});const raw=Buffer.from(JSON.stringify({id:'state-1',type:'whatsapp.smb.app.state.sync',whatsappSmbAppState:{}}));await assert.rejects(repository.admitSigned(raw,'t=1700000000,s='.padEnd(79,'0')),error=>error.code==='YCLOUD_WEBHOOK_UNAUTHORIZED');});
