const test=require("node:test");const assert=require("node:assert/strict");const crypto=require("node:crypto");const {CredentialVault,loadKey}=require("../src/connectors/vault");
test("credential vault round trips with context binding and no plaintext",()=>{const vault=new CredentialVault({key:crypto.randomBytes(32),keyId:"test-v1"});const encrypted=vault.encrypt({token:"top-secret",webhookSecret:"hook"},"connection-a");assert.equal(encrypted.keyId,"test-v1");assert.equal(encrypted.ciphertext.includes(Buffer.from("top-secret")),false);assert.deepEqual(vault.decrypt(encrypted.ciphertext,"connection-a"),{token:"top-secret",webhookSecret:"hook"});assert.throws(()=>vault.decrypt(encrypted.ciphertext,"connection-b"));});
test("credential vault rejects missing or short master keys",()=>{assert.throws(()=>loadKey({CONNECTOR_MASTER_KEY:"bad"}),/32-byte/);});
