const test=require('node:test');const assert=require('node:assert/strict');const {MetaOnboardingRepository}=require('../src/messaging/metaOnboardingRepository');test('status is workspace-scoped and credential-free',async()=>{const calls=[];const pool={async query(sql,params){calls.push(params);if(sql.includes('SELECT EXISTS'))return{rows:[{pending:true}]};return{rows:[{connection_id:'c',label:'Official',status:'connecting',number_id:'n',number_label:'Sales',phone:'+923001112222',automation_enabled:false}]};}};const s=await new MetaOnboardingRepository(pool).status('a','w');assert.equal(s.pending,true);assert.deepEqual(calls,[['a','w'],['w']]);assert.doesNotMatch(JSON.stringify(s),/encrypted|credential|token|external_session|settings/i);});
