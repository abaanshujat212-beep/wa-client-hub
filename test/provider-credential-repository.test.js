const test = require("node:test"); const assert = require("node:assert/strict"); const { ProviderCredentialRepository } = require("../src/messaging/providerCredentialRepository");
test("Meta credentials resolve only for the exact active tenant connection", async () => { const calls=[]; const encrypted=Buffer.from("encrypted"); const repository=new ProviderCredentialRepository({async query(sql,params){calls.push({sql,params});return{rows:[{id:"connection-a",workspace_id:"workspace-a",provider:"whatsapp_cloud",status:"active",encrypted_credentials:encrypted}]};}}, {decrypt(value,context){assert.equal(value,encrypted);assert.equal(context,"connection-a");return{accessToken:"secret-token",phoneNumberId:"123456789",businessAccountId:"waba-a"};}}); const result=await repository.resolveMeta({workspaceId:"workspace-a",providerConnectionId:"connection-a"}); assert.deepEqual(calls[0].params,["connection-a","workspace-a"]); assert.match(calls[0].sql,/provider='whatsapp_cloud'/); assert.match(calls[0].sql,/status='active'/); assert.deepEqual(result,{accessToken:"secret-token",phoneNumberId:"123456789",businessAccountId:"waba-a"}); });
test("Meta credential lookup does not disclose unavailable connections", async () => { const repository=new ProviderCredentialRepository({async query(){return{rows:[]};}}, {decrypt(){throw new Error("must not decrypt");}}); assert.equal(await repository.resolveMeta({workspaceId:"other",providerConnectionId:"connection-a"}),null); });
test("Meta credential validation rejects incomplete decrypted values", async () => { const repository=new ProviderCredentialRepository({async query(){return{rows:[{id:"connection-a",encrypted_credentials:Buffer.from("encrypted")}]};}}, {decrypt(){return{accessToken:"",phoneNumberId:"not-digits"};}}); await assert.rejects(repository.resolveMeta({workspaceId:"workspace-a",providerConnectionId:"connection-a"}),error=>error.code==="META_CREDENTIALS_INVALID"); });
