const { MetaSignupProtection } = require('./metaSignupProtection');
const { validateOrigin } = require('./metaSignupRoutes');
function enabled(env) { return env.META_SIGNUP_ENABLED === 'true'; }
function parseInterval(value) { if (value === undefined || value === '') return 300000; if (!/^\d+$/.test(String(value))) throw new Error('META_SIGNUP_CLEANUP_INTERVAL_MS must be an integer'); const interval = Number(value); if (!Number.isSafeInteger(interval) || interval < 60000 || interval > 3600000) throw new Error('META_SIGNUP_CLEANUP_INTERVAL_MS must be between 60000 and 3600000'); return interval; }
function parseMediaCleanupInterval(value) { if (value === undefined || value === '') return 3600000; if (!/^\d+$/.test(String(value))) throw new Error('META_MEDIA_CLEANUP_INTERVAL_MS must be an integer'); const interval = Number(value); if (!Number.isSafeInteger(interval) || interval < 300000 || interval > 86400000) throw new Error('META_MEDIA_CLEANUP_INTERVAL_MS must be between 300000 and 86400000'); return interval; }
function parseMediaCleanupBatch(value) { if (value === undefined || value === '') return 100; if (!/^\d+$/.test(String(value))) throw new Error('META_MEDIA_CLEANUP_BATCH_SIZE must be an integer'); const batch = Number(value); if (!Number.isSafeInteger(batch) || batch < 1 || batch > 100) throw new Error('META_MEDIA_CLEANUP_BATCH_SIZE must be between 1 and 100'); return batch; }
function validateMetaSignupConfig(env = process.env, store) { if (!enabled(env)) return { enabled: false }; const issues = []; if (store?.driver !== 'postgres' || typeof store?.repository?.pool?.query !== 'function') issues.push('PostgreSQL storage is required'); if (!validateOrigin(env.APP_ORIGIN)) issues.push('APP_ORIGIN must be one exact HTTPS origin'); if (!/^v\d+\.\d+$/.test(String(env.META_GRAPH_VERSION || ''))) issues.push('META_GRAPH_VERSION must be explicit'); if (!/^\d+$/.test(String(env.META_APP_ID || ''))) issues.push('META_APP_ID must contain digits'); if (String(env.META_APP_SECRET || '').length < 20) issues.push('META_APP_SECRET is missing or too short'); let interval; let mediaCleanupInterval; let mediaCleanupBatch; try { interval = parseInterval(env.META_SIGNUP_CLEANUP_INTERVAL_MS); } catch (error) { issues.push(error.message); } try { mediaCleanupInterval = parseMediaCleanupInterval(env.META_MEDIA_CLEANUP_INTERVAL_MS); } catch (error) { issues.push(error.message); } try { mediaCleanupBatch = parseMediaCleanupBatch(env.META_MEDIA_CLEANUP_BATCH_SIZE); } catch (error) { issues.push(error.message); } if (issues.length) throw new Error(`Meta signup configuration error: ${issues.join('; ')}`); return { enabled: true, origin: env.APP_ORIGIN, interval, mediaCleanupInterval, mediaCleanupBatch }; }
function createMetaSignupRuntime({ env = process.env, store, logger = console, fetchImpl = globalThis.fetch } = {}) { const config = validateMetaSignupConfig(env, store); const hidden = (_req, res) => res.status(404).json({ error: 'Not found' }); if (!config.enabled) return { enabled: false, router: hidden, connectionsRouter: hidden, templatesRouter: hidden, mediaRouter: hidden, start() {}, stop() {}, status: () => ({ enabled: false, cleanup: 'disabled', mediaCleanup: 'disabled' }) }; const { MetaEmbeddedSignupService } = require('./metaEmbeddedSignupService'); const { createMetaSignupRouter } = require('./metaSignupRoutes'); const { createMetaConnectionRouter } = require('./metaConnectionRoutes'); const { createMetaTemplateSyncRouter } = require('./metaTemplateSyncRoutes'); const { createMetaMediaRouter } = require('./metaMediaRoutes'); const { MetaConnectionLifecycleRepository } = require('./metaConnectionLifecycleRepository'); const { MetaConnectionDiagnosticsService } = require('./metaConnectionDiagnosticsService'); const { MetaGraphClient } = require('./metaGraphClient'); const { MetaTemplateSyncRepository } = require('./metaTemplateSyncRepository'); const { MetaTemplateSyncService } = require('./metaTemplateSyncService'); const { MetaMediaRepository } = require('./metaMediaRepository'); const { MetaMediaService } = require('./metaMediaService'); const { MetaMediaRetentionService } = require('./metaMediaRetentionService'); const { CredentialVault } = require('../security/credentialVault'); const pool = store.repository.pool; const signupService = new MetaEmbeddedSignupService({ graphVersion: env.META_GRAPH_VERSION, appId: env.META_APP_ID, appSecret: env.META_APP_SECRET }); signupService.assertConfigured(); const vault = new CredentialVault({ env }); const protection = new MetaSignupProtection(pool); const repository = new MetaConnectionLifecycleRepository(pool, vault); const diagnostics = new MetaConnectionDiagnosticsService({ repository, graphVersion: env.META_GRAPH_VERSION }); const graphClient = new MetaGraphClient({ graphVersion: env.META_GRAPH_VERSION, fetchImpl }); const templateRepository = new MetaTemplateSyncRepository(pool, vault); const templateService = new MetaTemplateSyncService({ repository: templateRepository, graphClient }); const mediaRepository = new MetaMediaRepository(pool, vault); const mediaService = new MetaMediaService({ graphClient }); const mediaRetention = new MetaMediaRetentionService({ repository: mediaRepository, mediaService, maxBatch: config.mediaCleanupBatch }); const router = createMetaSignupRouter({ enabled: true, pool, signupService, vault, origin: config.origin, publicConfig: { appId: env.META_APP_ID, configId: env.META_EMBEDDED_SIGNUP_CONFIG_ID, graphVersion: env.META_GRAPH_VERSION } }); const connectionsRouter = createMetaConnectionRouter({ enabled: true, pool, repository, diagnostics, origin: config.origin }); const templatesRouter = createMetaTemplateSyncRouter({ enabled: true, pool, service: templateService, origin: config.origin }); const mediaRouter = createMetaMediaRouter({ enabled: true, pool, repository: mediaRepository, service: mediaService, origin: config.origin }); let timer = null; let mediaCleanupTimer = null; let running = false; let mediaCleanupRunning = false; let lastSuccessAt = null; let lastFailureAt = null; let lastMediaCleanupSuccessAt = null; let lastMediaCleanupFailureAt = null; async function cleanup() { if (running) return; running = true; try { const counts = await protection.cleanup(500); lastSuccessAt = new Date().toISOString(); logger.info?.('Meta signup cleanup completed', counts); } catch { lastFailureAt = new Date().toISOString(); logger.error?.('Meta signup cleanup failed'); } finally { running = false; } } async function cleanupMedia() { if (mediaCleanupRunning) return; mediaCleanupRunning = true; try { const counts = await mediaRetention.cleanup({ limit: config.mediaCleanupBatch }); lastMediaCleanupSuccessAt = new Date().toISOString(); logger.info?.('Meta media cleanup completed', counts); } catch { lastMediaCleanupFailureAt = new Date().toISOString(); logger.error?.('Meta media cleanup failed'); } finally { mediaCleanupRunning = false; } } return { enabled: true, router, connectionsRouter, templatesRouter, mediaRouter, start() { if (timer || mediaCleanupTimer) return; void cleanup(); void cleanupMedia(); timer = setInterval(() => void cleanup(), config.interval); mediaCleanupTimer = setInterval(() => void cleanupMedia(), config.mediaCleanupInterval); timer.unref?.(); mediaCleanupTimer.unref?.(); }, stop() { if (timer) clearInterval(timer); if (mediaCleanupTimer) clearInterval(mediaCleanupTimer); timer = null; mediaCleanupTimer = null; }, status: () => ({ enabled: true, cleanup: running ? 'running' : 'scheduled', mediaCleanup: mediaCleanupRunning ? 'running' : 'scheduled', lastSuccessAt, lastFailureAt, lastMediaCleanupSuccessAt, lastMediaCleanupFailureAt }), cleanup, cleanupMedia }; }
module.exports = { createMetaSignupRuntime, validateMetaSignupConfig, parseInterval, parseMediaCleanupInterval, parseMediaCleanupBatch, enabled };
