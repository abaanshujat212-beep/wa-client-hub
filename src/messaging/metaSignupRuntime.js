const{MetaSignupProtection}=require('./metaSignupProtection');const{validateOrigin}=require('./metaSignupRoutes');function enabled(env){return env.META_SIGNUP_ENABLED==='true';}function parseInterval(v){if(v===undefined||v==='')return 300000;if(!/^\d+$/.test(String(v)))throw new Error('META_SIGNUP_CLEANUP_INTERVAL_MS must be an integer');const n=Number(v);if(!Number.isSafeInteger(n)||n<60000||n>3600000)throw new Error('META_SIGNUP_CLEANUP_INTERVAL_MS must be between 60000 and 3600000');return n;}function validateMetaSignupConfig(env=process.env,store){if(!enabled(env))return{enabled:false};const issues=[];if(store?.driver!=='postgres'||typeof store?.repository?.pool?.query!=='function')issues.push('PostgreSQL storage is required');if(!validateOrigin(env.APP_ORIGIN))issues.push('APP_ORIGIN must be one exact HTTPS origin');if(!/^v\d+\.\d+$/.test(String(env.META_GRAPH_VERSION||'')))issues.push('META_GRAPH_VERSION must be explicit');if(!/^\d+$/.test(String(env.META_APP_ID||'')))issues.push('META_APP_ID must contain digits');if(String(env.META_APP_SECRET||'').length<20)issues.push('META_APP_SECRET is missing or too short');let interval;try{interval=parseInterval(env.META_SIGNUP_CLEANUP_INTERVAL_MS);}catch(e){issues.push(e.message);}if(issues.length)throw new Error(`Meta signup configuration error: ${issues.join('; ')}`);return{enabled:true,origin:env.APP_ORIGIN,interval};}function createMetaSignupRuntime({env=process.env,store,logger=console}={}){const c=validateMetaSignupConfig(env,store);if(!c.enabled)return{enabled:false,router:(_q,r)=>r.status(404).json({error:'Not found'}),start(){},stop(){},status:()=>({enabled:false,cleanup:'disabled'})};const{MetaEmbeddedSignupService}=require('./metaEmbeddedSignupService'),{createMetaSignupRouter}=require('./metaSignupRoutes'),{CredentialVault}=require('../connectors/vault'),pool=store.repository.pool,service=new MetaEmbeddedSignupService({graphVersion:env.META_GRAPH_VERSION,appId:env.META_APP_ID,appSecret:env.META_APP_SECRET}),vault=new CredentialVault({env}),protection=new MetaSignupProtection(pool);service.assertConfigured();const router=createMetaSignupRouter({enabled:true,pool,signupService:service,vault,origin:c.origin,publicConfig:{appId:env.META_APP_ID,configId:env.META_EMBEDDED_SIGNUP_CONFIG_ID,graphVersion:env.META_GRAPH_VERSION}});let timer=null,running=false,lastSuccessAt=null,lastFailureAt=null;async function cleanup(){if(running)return;running=true;try{const counts=await protection.cleanup(500);lastSuccessAt=new Date().toISOString();logger.info?.('Meta signup cleanup completed',counts);}catch{lastFailureAt=new Date().toISOString();logger.error?.('Meta signup cleanup failed');}finally{running=false;}}return{enabled:true,router,start(){if(timer)return;void cleanup();timer=setInterval(()=>void cleanup(),c.interval);timer.unref?.();},stop(){if(timer)clearInterval(timer);timer=null;},status:()=>({enabled:true,cleanup:running?'running':'scheduled',lastSuccessAt,lastFailureAt}),cleanup};}module.exports={createMetaSignupRuntime,validateMetaSignupConfig,parseInterval,enabled};
