const crypto=require('node:crypto');const{assertOfficialTextPolicy,MessagePolicyError}=require('./messagePolicy');
class SendError extends Error{constructor(message,{code='SEND_FAILED',status=500}={}){super(message);this.name='SendError';this.code=code;this.status=status;}}
function normalizeIdempotencyKey(value){const key=String(value||'').trim();if(!key||key.length>200)throw new SendError('A valid idempotency key is required',{code:'INVALID_IDEMPOTENCY_KEY',status:400});return key;}function normalizeText(value){const text=String(value||'').trim();if(!text||text.length>4096)throw new SendError('Message text must contain 1 to 4096 characters',{code:'INVALID_MESSAGE_TEXT',status:400});return text;}function normalizeOrigin(value){const origin=String(value||'api');if(!['api','campaign','crm'].includes(origin))throw new SendError('Message origin is invalid',{code:'INVALID_MESSAGE_ORIGIN',status:400});return origin;}
class CanonicalSendService{constructor({repository,adapters,policy=null,events=null,audit=null}){if(!repository)throw new TypeError('repository is required');this.repository=repository;this.adapters=new Map(Object.entries(adapters||{}));this.policy=policy;this.events=events;this.audit=audit;}
 async sendText({actorId,workspaceIds,conversationId,text,idempotencyKey,origin='api'}){const key=normalizeIdempotencyKey(idempotencyKey),body=normalizeText(text),messageOrigin=normalizeOrigin(origin),dispatch=await this.repository.resolveConversationDispatch({workspaceIds,conversationId});if(!dispatch)throw new SendError('Conversation not found',{code:'CONVERSATION_NOT_FOUND',status:404});if(!dispatch.automationEnabled)throw new SendError('Automation is not enabled for this WhatsApp number',{code:'AUTOMATION_DISABLED',status:409});if(dispatch.providerStatus!=='active')throw new SendError('Messaging provider connection is not active',{code:'PROVIDER_CONNECTION_INACTIVE',status:409});const adapter=this.adapters.get(dispatch.provider);if(!adapter)throw new SendError(`Messaging provider '${dispatch.provider}' is not available`,{code:'PROVIDER_UNAVAILABLE',status:409});try{assertOfficialTextPolicy(dispatch.provider,this.policy?await this.policy.evaluateText({dispatch}):null);}catch(error){if(error instanceof MessagePolicyError)throw new SendError(error.message,{code:error.code,status:error.status});throw error;}const requestHash=crypto.createHash('sha256').update(JSON.stringify({conversationId:dispatch.conversationId,type:'text',body,origin:messageOrigin})).digest('hex'),reservation=await this.repository.reserveOutbound({dispatch,idempotencyKey:key,requestHash});if(!reservation.created&&reservation.attempt.request_hash!==requestHash)throw new SendError('Idempotency key was already used for a different request',{code:'IDEMPOTENCY_KEY_REUSED',status:409});if(!reservation.created){const attempt=reservation.attempt;if(attempt.status==='failed')throw new SendError('The previous send attempt failed; use a new idempotency key after review',{code:'IDEMPOTENT_SEND_FAILED',status:409});if(attempt.status==='provider_accepted'&&!attempt.message_id){const recovered=await this.repository.recordOutbound({attemptId:attempt.id,dispatch,body,type:'text',origin:messageOrigin,externalMessageId:attempt.external_message_id||null,idempotencyKey:key,rawProviderStatus:attempt.raw_provider_status||null});this.events?.publish(dispatch.workspaceId,'message.changed',{conversationId:dispatch.conversationId,messageId:recovered.id,provider:dispatch.provider,numberId:dispatch.numberId});if(this.audit)await this.audit(actorId,'message.send_recovered',{workspaceId:dispatch.workspaceId,conversationId:dispatch.conversationId,providerConnectionId:dispatch.providerConnectionId,messageId:recovered.id,attemptId:attempt.id,origin:messageOrigin});return{message:recovered,duplicate:true,recovered:true};}return{message:attempt.message_id?{id:attempt.message_id,externalMessageId:attempt.external_message_id,status:attempt.message_status||attempt.status}:{id:null,attemptId:attempt.id,status:attempt.status},duplicate:true};}const attemptId=reservation.attempt.id;await this.repository.markDispatching({attemptId,workspaceId:dispatch.workspaceId});let providerResult;try{providerResult=await adapter.sendText({connection:dispatch,to:dispatch.contactPhone,text:body,idempotencyKey:key});}catch(error){await this.repository.failOutbound({attemptId,workspaceId:dispatch.workspaceId,error});throw new SendError('Provider rejected the message',{code:error.code||'PROVIDER_SEND_FAILED',status:502});}await this.repository.markProviderAccepted({attemptId,workspaceId:dispatch.workspaceId,externalMessageId:providerResult?.externalMessageId||null,rawProviderStatus:providerResult?.rawStatus||null});const message=await this.repository.recordOutbound({attemptId,dispatch,body,type:'text',origin:messageOrigin,externalMessageId:providerResult?.externalMessageId||null,idempotencyKey:key,rawProviderStatus:providerResult?.rawStatus||null});this.events?.publish(dispatch.workspaceId,'message.changed',{conversationId:dispatch.conversationId,messageId:message.id,provider:dispatch.provider,numberId:dispatch.numberId});if(this.audit)await this.audit(actorId,'message.sent',{workspaceId:dispatch.workspaceId,conversationId:dispatch.conversationId,numberId:dispatch.numberId,providerConnectionId:dispatch.providerConnectionId,provider:dispatch.provider,messageId:message.id,origin:messageOrigin});return{message,duplicate:false};}}
module.exports={CanonicalSendService,SendError,normalizeIdempotencyKey,normalizeText,normalizeOrigin};
