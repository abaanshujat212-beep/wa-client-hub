const{MetaGraphClient,validVersion}=require('./metaGraphClient');
function providerError(message,code,status){const error=new Error(message);error.code=code;if(status)error.providerStatus=status;return error;}
function normalizeRecipient(value){const recipient=String(value||'').replace(/\D/g,'');if(!/^\d{8,15}$/.test(recipient))throw providerError('Invalid WhatsApp recipient','INVALID_RECIPIENT');return recipient;}
class MetaCloudApiAdapter{constructor({credentialResolver,graphVersion,fetchImpl=globalThis.fetch,baseUrl='https://graph.facebook.com',graphClient=null,timeoutMs,maxRetries}={}){if(!credentialResolver||typeof credentialResolver.resolveMeta!=='function')throw new TypeError('credentialResolver.resolveMeta is required');this.credentialResolver=credentialResolver;this.graphVersion=String(graphVersion||'').trim();this.graphClient=graphClient||new MetaGraphClient({graphVersion,fetchImpl,baseUrl,timeoutMs,maxRetries});this.ownsGraphClient=!graphClient;}
 async sendText({connection,to,text}){if(connection.provider!=='whatsapp_cloud')throw providerError('Meta adapter received a non-Meta connection','PROVIDER_MISMATCH');if(this.ownsGraphClient)validVersion(this.graphVersion);const recipient=normalizeRecipient(to),credentials=await this.credentialResolver.resolveMeta({workspaceId:connection.workspaceId,providerConnectionId:connection.providerConnectionId});if(!credentials)throw providerError('Meta provider connection is unavailable','META_CONNECTION_UNAVAILABLE');if(String(connection.externalSessionId||'')!==credentials.phoneNumberId)throw providerError('Meta phone-number mapping does not match the conversation','PROVIDER_MAPPING_MISMATCH');const payload=await this.graphClient.request({path:[credentials.phoneNumberId,'messages'],accessToken:credentials.accessToken,method:'POST',body:{messaging_product:'whatsapp',recipient_type:'individual',to:recipient,type:'text',text:{preview_url:false,body:text}}});const externalMessageId=String(payload?.messages?.[0]?.id||'').trim();if(!externalMessageId)throw providerError('Meta Cloud API response did not include a message ID','META_RESPONSE_INVALID');return{externalMessageId,rawStatus:'accepted'};}}
module.exports={MetaCloudApiAdapter,normalizeRecipient};
