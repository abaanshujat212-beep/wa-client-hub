class MetaWebhookWorker{
 constructor({repository,intervalMs=1000,batchSize=10,logger=console,onInboundMessage=null}){if(typeof repository?.claim!=='function'||typeof repository?.processReceipt!=='function')throw new TypeError('Meta webhook repository is required');this.repository=repository;this.intervalMs=Math.max(250,Number(intervalMs)||1000);this.batchSize=Math.min(50,Math.max(1,Number(batchSize)||10));this.logger=logger;this.onInboundMessage=onInboundMessage;this.timer=null;this.running=false;}
 async tick(){if(this.running)return;this.running=true;try{const receipts=await this.repository.claim(this.batchSize);for(const receipt of receipts){try{const result=await this.repository.processReceipt(receipt);const p=receipt.payload||{};if(result?.messageId&&p.direction==='inbound'&&!p.echo&&this.onInboundMessage)await this.onInboundMessage({workspaceId:receipt.workspace_id,numberId:p.numberId,providerMessageId:p.providerMessageId,canonicalMessageId:result.messageId,source:'meta',echo:Boolean(p.echo)});}catch(error){await this.repository.fail(receipt,error);this.logger.error?.('Meta webhook receipt processing failed',{receiptId:receipt.id,code:error?.code||'META_WEBHOOK_PROCESSING_FAILED'});}}}catch{this.logger.error?.('Meta webhook worker poll failed');}finally{this.running=false;}}
 start(){if(this.timer)return;void this.tick();this.timer=setInterval(()=>void this.tick(),this.intervalMs);this.timer.unref?.();}
 stop(){if(this.timer)clearInterval(this.timer);this.timer=null;}
}
module.exports={MetaWebhookWorker};
