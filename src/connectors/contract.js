const ALLOWED_TYPES=new Set(["contact.created","contact.updated","order.created","order.updated","conversation.created","conversation.updated","message.received","message.sent"]);
function validateEvent(event){if(!event||typeof event!=="object")throw new Error("Event object is required");for(const key of ["externalEventId","type","subjectType","subjectId","occurredAt","data"])if(event[key]===undefined||event[key]===null||event[key]==="")throw new Error(`Event ${key} is required`);if(!ALLOWED_TYPES.has(event.type))throw new Error("Unsupported connector event type");const occurredAt=new Date(event.occurredAt);if(Number.isNaN(occurredAt.getTime()))throw new Error("Event occurredAt is invalid");if(typeof event.data!=="object"||Array.isArray(event.data))throw new Error("Event data must be an object");return{...event,externalEventId:String(event.externalEventId),subjectType:String(event.subjectType),subjectId:String(event.subjectId),occurredAt:occurredAt.toISOString()};}
function mapFields(data,mapping={}){const output={};for(const [remote,local] of Object.entries(mapping))if(Object.hasOwn(data,remote)&&typeof local==="string"&&local)output[local]=data[remote];return output;}
module.exports={ALLOWED_TYPES,validateEvent,mapFields};
